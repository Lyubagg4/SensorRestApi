# 🌡️ Sensor REST API (Spring Boot)

RESTful веб-сервис для сбора и анализа метеорологических данных с IoT-датчиков. Сервис предоставляет чистое API для регистрации датчиков, записи измерений (температура, дождь) и получения аналитики.

---

## 🛠️ Технологический стек (Tech Stack)

*   **Backend:** Java 17, Spring Boot 3+
*   **API:** REST, JSON
*   **Database:** PostgreSQL, Spring Data JPA (Hibernate)
*   **Validation:** Bean Validation (Hibernate Validator)
*   **Object Mapping:** MapStruct
*   **Exception Handling:** Global `@RestControllerAdvice`
*   **Testing:** JUnit 5, Mockito, `@WebMvcTest`
*   **Logging:** SLF4J
*   **Build Tool:** Maven
*   **Containerization:** Docker, Docker Compose

## ✨ Ключевые возможности (Features)

*   **📝 Регистрация сенсоров:** Создание датчиков с уникальными именами.
*   **📊 Добавление измерений:** Запись показаний температуры (от -100 до 100) и факта дождя.
*   **📈 Получение данных:** API для получения всех измерений и подсчета дождливых дней.
*   **🛡️ Промышленное качество API:**
    *   **Валидация DTO:** Автоматическая проверка входящих данных (`@Valid`).
    *   **Глобальная обработка ошибок:** Единый формат ответов об ошибках (400, 500).
    *   **Семантичное логирование:** Логирование ключевых событий в жизненном цикле запроса.
    *   **Чистая архитектура:** Разделение на DTO, Entity, Mappers (MapStruct), Controllers, Services.

## 🏗️ Архитектура

*   **Архитектура:** Многослойная REST API (Controller-Service-Repository).
*   **Слои:**
    *   **DTO:** Для передачи данных по сети.
    *   **Entity:** JPA-сущности для отображения на БД.
    *   **Mapper (MapStruct):** Для конвертации между DTO и Entity.
    *   **Service:** Бизнес-логика и валидация.
    *   **Controller:** Обработка HTTP-запросов и ответов.
    *   **Repository:** Доступ к данным через Spring Data JPA.

## 🚀 Запуск приложения (Docker)

1.  **Клонируйте репозиторий.**
2.  **Запустите команду из корневой директории проекта:**
    ```bash
    docker-compose up --build
    ```
3.  Сервис будет доступен по адресу: `http://localhost:8080`

## 🧪 Тестирование

Проект полностью покрыт тестами, что является best practice для REST API:
*   **`MeasurementsControllerTest`, `SensorControllerTest`:** Проверка HTTP-статусов, валидации входных данных и формата JSON-ответов с помощью `MockMvc`.
*   **`MeasureServiceTest`, `SensorServiceTest`:** Unit-тесты бизнес-логики с мокированием репозиториев.

## 📡 API Endpoints (Полная спецификация)

### 🌡️ Управление измерениями (`/measurements`)

| Метод | Путь | Описание | Тело запроса (JSON) | Успешный ответ |
|-------|------|-----------|---------------------|----------------|
| GET | `/measurements` | Получить все измерения | - | `200 OK`, `List<MeasurementsDTO>` |
| GET | `/measurements/rainyDaysCount` | Количество дней с дождём | - | `200 OK`, `Integer` |
| POST | `/measurements/add` | Добавить новое измерение | ```json { "value": 25.5, "raining": true, "sensor": { "name": "sensor123" } } ``` | `200 OK` |

### 📟 Управление сенсорами (`/sensors`)

| Метод | Путь | Описание | Тело запроса (JSON) | Успешный ответ |
|-------|------|-----------|---------------------|----------------|
| POST | `/sensors/registration` | Регистрация нового сенсора | ```json { "name": "sensor123" } ``` | `200 OK` |

### ⚠️ Обработка ошибок (Глобальная)

Все ошибки возвращаются в едином формате:
```json
{
    "message": "Описание ошибки",
    "timestamp": "2024-01-15T10:30:00",
    "errors": {
        "fieldName": "Сообщение об ошибке валидации"
    }
}